import { VerticalBox } from "std-widgets.slint";

export component VerticalBoxComponent {
    VerticalBox {
        Rectangle {
            height: 100px;
            background: #f2f2f2;
            Text {
                color: black;
                font-size: 16px;
                font-weight: 500;
            }
        }
    }
}