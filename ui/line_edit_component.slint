import { LineEdit } from "std-widgets.slint";

export component LineEditComponent {
    LineEdit {
        horizontal-alignment: center;
        font-size: 14px;
        placeholder-text: "Enter income";
        height: 35px;
    }
}